# Optional. Production (`npm run build`): set so `/create/qa-posters` shows a passphrase form
# instead of redirecting home. Omit entirely to disable that URL on the deployed site.
# Dev (`npm run dev`): QA stays open without a passphrase; this variable is ignored for access.
# VITE_POSTER_QA_SECRET=use-a-long-random-phrase
